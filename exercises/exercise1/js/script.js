/**************************************************
Placeholder Project
Pippin Barr

This is a project I can display when I create example links
to show how to submit things!
**************************************************/

// setup()
//
// Make a canvas!
function setup() {
  createCanvas(windowWidth, windowHeight);

}

// draw()
//
// Display some text!
function draw() {
  background(0);
  fill(255);
  stroke(255, 0, 0);
  strokeWeight(100);
  textAlign(CENTER, CENTER);
  textSize(32);
  text("Placeholder project loves you very much.", width / 2, height / 2);
}